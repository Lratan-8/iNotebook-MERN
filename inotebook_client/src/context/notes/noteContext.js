import {createContext} from 'react';

const noteContext = createContext();  //this is the syntax to create a new context
//context will basically hold all the state related to the notes


export default noteContext; 